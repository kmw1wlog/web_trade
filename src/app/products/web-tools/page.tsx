import { ProductDetail } from "@/app/products/ProductDetail";

export const metadata = { title: "매매기록 기능웹 | 투자 루틴 스케일링" };

export default function WebToolsPage() {
  return <ProductDetail slug="web-tools" />;
}
